appenders:
  my_stdout:
    kind: console
  requests:
    kind: file
    path: "log.log"
    encoder:
      pattern: "{h({d(%Y-%m-%d %H:%M:%S)(utc)} - {l}: {m}{n})}"
root:
  level: debug
  appenders:
    - my_stdout